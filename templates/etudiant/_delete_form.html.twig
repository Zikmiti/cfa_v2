<form method="post" class="d-inline" action="{{ path('etudiant_delete', {'id': etudiant.id}) }}" onsubmit="return confirm('Confirmer la suppression de l'étudiant ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etudiant.id) }}" >
    <button class="btn btn-danger text-white">
        <i class="fas fa-trash-alt"></i>
    </button>
</form>
