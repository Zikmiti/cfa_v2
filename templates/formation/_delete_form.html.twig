<form method="post" class="d-inline" action="{{ path('etudiant_delete', {'id': formation.id}) }}" id="formDelete" >
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formation.id) }}" >
    <button class="btn btn-warning" onclick="archiveFunction()">Supprimer</button>
</form>
